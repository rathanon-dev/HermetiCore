# Gemini MCP Usage Guide
This plugin connects the GitHub MCP server to Antigravity (Gemini).
Cookie Token Usage:
- The token is managed via config/.env using GITHUB_PERSONAL_ACCESS_TOKEN.
- AI Agents reading this directory should use the token to authenticate GitHub API requests.
