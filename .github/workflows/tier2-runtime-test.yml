# ARCHIVED — superseded by hermeticore-ci.yml
# This workflow is disabled. Use hermeticore-ci.yml instead.
name: "[ARCHIVED] Tier 2 Runtime Scaffold Test"
on:
  workflow_dispatch:
    inputs:
      reason:
        description: "Reason for manual trigger"
        required: true
jobs:
  archived-notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is archived. Use hermeticore-ci.yml"
