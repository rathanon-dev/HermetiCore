# Bob — Demo User Profile
# Playwright userDataDir for multi-user simulation (User B)
# Usage: await browser.newContext({ userDataDir: './sandbox/demo_users/user_bob/profile' })
# The profile/ folder is gitignored — only this README is tracked.
