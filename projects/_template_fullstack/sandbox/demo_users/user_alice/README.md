# Alice — Demo User Profile
# Playwright userDataDir for multi-user simulation (User A)
# Usage: await browser.newContext({ userDataDir: './sandbox/demo_users/user_alice/profile' })
# The profile/ folder is gitignored — only this README is tracked.
